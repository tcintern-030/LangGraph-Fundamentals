# LangGraph-Fundamentals
This project demonstrates a basic LangGraph workflow using three connected nodes. A shared state is passed from one node to the next, where each node updates the message and prints the current state. It helps illustrate the core concepts of LangGraph, including nodes, edges, state management, and sequential workflow execution.
